/**
 * Export (JSON + HTML bookmarks) / import (JSON) dialog. All local: Blob +
 * URL.createObjectURL + <a download>. No network.
 * NOTE: scaffold stub — implemented in Phase 7.
 */
export function ExportImportDialog() {
  return null;
}
