/**
 * First-run intro explaining the capture -> file -> close -> reopen loop and
 * the zero-network guarantee.
 * NOTE: scaffold stub — implemented in Phase 6/7.
 */
export function FirstRunIntro() {
  return null;
}
